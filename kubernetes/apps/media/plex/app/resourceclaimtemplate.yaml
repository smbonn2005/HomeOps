---
# yaml-language-server: $schema=https://cluster-schemas.pages.dev/resource.k8s.io/resourceclaimtemplate_v1.json
apiVersion: resource.k8s.io/v1
kind: ResourceClaimTemplate
metadata:
  name: plex
spec:
  spec:
    devices:
      requests:
        - name: gpu
          exactly:
            deviceClassName: gpu.intel.com
