---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: mysql-operator
spec:
  chart:
    spec:
      chart: mysql-operator
      version: "2.3.0"
      sourceRef:
        kind: HelmRepository
        name: mysql-operator
  interval: 30m
