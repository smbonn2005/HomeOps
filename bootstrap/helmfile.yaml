---
# yaml-language-server: $schema=https://json.schemastore.org/helmfile

helmDefaults:
  wait: true
  waitForJobs: true

repositories:
  - name: cilium
    url: https://helm.cilium.io

  - name: jetstack
    url: https://charts.jetstack.io

releases:
  - name: cilium
    namespace: kube-system
    chart: cilium/cilium
    version: 1.17.1
    values: ['{{ requiredEnv "ROOT_DIR" }}/apps/kube-system/cilium/app/helm-values.yaml']
    hooks:
      - # Wait for cilium CRDs to be available
        events: ['postsync']
        command: bash
        args:
          - -c
          - until kubectl get crd ciliuml2announcementpolicies.cilium.io ciliumloadbalancerippools.cilium.io &>/dev/null; do sleep 10; done
        showlogs: true
      - # Apply cilium network configuration
        events: ['postsync']
        command: kubectl
        args:
          - apply
          - --namespace=kube-system
          - --server-side
          - --field-manager=kustomize-controller
          - --filename={{ requiredEnv "ROOT_DIR" }}/kubernetes/apps/kube-system/cilium/app/networks.yaml
        showlogs: true

  - name: coredns
    namespace: kube-system
    chart: oci://ghcr.io/coredns/charts/coredns
    version: 1.39.1
    values: ['{{ requiredEnv "ROOT_DIR" }}/apps/kube-system/coredns/app/helm-values.yaml']
    needs: ['kube-system/cilium']

  - name: spegel
    namespace: kube-system
    chart: oci://ghcr.io/spegel-org/helm-charts/spegel
    version: v0.0.30
    values: ['{{ requiredEnv "ROOT_DIR" }}/apps/kube-system/spegel/app/helm-values.yaml']
    needs: ['kube-system/coredns']

  - name: cert-manager
    namespace: cert-manager
    chart: jetstack/cert-manager
    version: v1.17.1
    values: ['{{ requiredEnv "ROOT_DIR" }}/apps/cert-manager/cert-manager/app/helm-values.yaml']
    needs: ['kube-system/spegel']

  - name: external-secrets
    namespace: kube-system
    chart: oci://ghcr.io/external-secrets/charts/external-secrets
    version: 0.14.3
    values: ['{{ requiredEnv "ROOT_DIR" }}/apps/kube-system/external-secrets/app/helm-values.yaml']
    needs: ['cert-manager/cert-manager']

  - name: flux-operator
    namespace: flux-system
    chart: oci://ghcr.io/controlplaneio-fluxcd/charts/flux-operator
    version: 0.17.0
    values: ['{{ requiredEnv "ROOT_DIR" }}/apps/flux-system/flux-operator/app/helm-values.yaml']
    needs: ['kube-system/external-secrets']

  - name: flux-instance
    namespace: flux-system
    chart: oci://ghcr.io/controlplaneio-fluxcd/charts/flux-instance
    version: 0.17.0
    values: ['{{ requiredEnv "ROOT_DIR" }}/apps/flux-system/flux-operator/instance/helm-values.yaml']
    needs: ['flux-system/flux-operator']
